# Direct dependencies for personal-rag (macOS / x86, Python 3.10)
#
# This file lists only the packages directly imported by the project scripts.
# Use it to regenerate the full pinned lockfile:
#
#   uv pip compile requirements-direct.txt --python-version 3.10 -o requirements.txt
#
# For Jetson (aarch64, JetPack 6.2), use requirements-jetson.txt instead.
# PyTorch must be installed first from the Jetson AI Lab index:
#   pip install torch torchvision --index-url https://pypi.jetson-ai-lab.io/jp6/cu126

chromadb==0.6.3
# Provides both the bi-encoder embedder AND the CrossEncoder reranker used in
# query.py (cross-encoder/ms-marco-MiniLM-L-6-v2, ~80 MB). No separate package;
# the reranker model is fetched from HF at first use (must be in the Jetson HF
# cache for offline runs).
sentence-transformers==3.3.1
python-frontmatter==1.3.0
PyYAML==6.0.3
pypdf==6.12.2
PyMuPDF>=1.24
cryptography>=3.1
torch==2.5.1
posthog==7.17.0
python-dotenv==1.2.2

# HTTP backend (rag-serve). fastapi/uvicorn already resolve transitively via
# chromadb; declaring them direct makes the API's genuine imports explicit.
# pyjwt is a new direct dep for the HS256 bearer-token auth layer.
fastapi
uvicorn[standard]
pyjwt
