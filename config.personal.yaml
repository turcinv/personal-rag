# Personal/Jetson profile. The historical config.yaml remains the canonical
# personal corpus configuration; this overlay avoids maintaining a second copy.
# Select with: RAG_CONFIG_PATH=./config.personal.yaml <rag-command>
extends: config.yaml
